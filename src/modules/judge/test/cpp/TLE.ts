const input = `
`;
const output = `
`;
const usrCode = `
#include <bits/stdc++.h>
int main(void) {
    while(1);
    return 0;
}
`;


const expectResult = 'time-limit-exceeded'

export default ({
  name: "cpp-TLE-1",
  usrCode,
  input,
  output,
  expectResult
})